As mentioned in [[CSS]], it's used to personalise the visual aspects of an HTML document.

[[HTML]] is used to mark up documents, ensuring they can be readable on a browser; it makes headings larger than regular text, paragraphs break onto new lines and have space between them, links are coloured and underlined to make them visually different from the rest of the text, etc.

While functional, this is incredibly basic and dull.
