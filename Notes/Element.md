An [[HTML]] element is part of a website, and usually includes an *opening tag* often including *attributes*, enclosed text and a closing tag.

```html
<p class="nice">Hello world!</p>
```

Elements differ from tags in that tags begin and end an element in the actual source code, whereas elements are part of the [[DOM]].